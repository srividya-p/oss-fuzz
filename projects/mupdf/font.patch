diff --git a/source/pdf/pdf-interpret.c b/source/pdf/pdf-interpret.c
index 7dfa36478..2c08fb311 100644
--- a/source/pdf/pdf-interpret.c
+++ b/source/pdf/pdf-interpret.c
@@ -114,26 +114,7 @@ pdf_clear_stack(fz_context *ctx, pdf_csi *csi)
 static pdf_font_desc *
 pdf_try_load_font(fz_context *ctx, pdf_document *doc, pdf_obj *rdb, pdf_obj *font, fz_cookie *cookie)
 {
-	pdf_font_desc *desc = NULL;
-	fz_try(ctx)
-		desc = pdf_load_font(ctx, doc, rdb, font);
-	fz_catch(ctx)
-	{
-		if (fz_caught(ctx) == FZ_ERROR_TRYLATER)
-		{
-			fz_ignore_error(ctx);
-			if (cookie)
-				cookie->incomplete++;
-		}
-		else
-		{
-			fz_rethrow_if(ctx, FZ_ERROR_SYSTEM);
-			fz_report_error(ctx);
-		}
-	}
-	if (desc == NULL)
-		desc = pdf_load_hail_mary_font(ctx, doc);
-	return desc;
+	return pdf_load_hail_mary_font(ctx, doc);
 }
 
 static fz_image *
